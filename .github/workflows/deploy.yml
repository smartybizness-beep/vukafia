name: Deploy to Railway

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # Railway auto-deploy from GitHub is enabled, manual action not needed
      # If manual deploy needed, configure proper RAILWAY_TOKEN and use official CLI
